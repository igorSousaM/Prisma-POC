export type ErrorHandling = {
  type: string;
  message: string;
};

export type PlateInput = {
  name: string;
  price: number;
  description: string;
  cookingTime: number;
  type: string;
};

export type ClientInput = {
  name: string;
  phone: string;
  address: string;
};
